## Nonogram solver with genetic algorithms 

Testing web service for the application of a genetic algorithm to solve nonograms.

This program requires know grid based nonogram solutions, whether stored on a csv file or draw on the front-end at `index.html`. Hint based solving not supported.

## How to run
```
export FLASK_APP=project/__init__.py
python3 manage.py run
```